{
  "name": "serverless-stack-todos-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  },
  "devDependencies": {
    "@tsconfig/node14": "^1.0.1",
    "@types/aws-lambda": "^8.10.101",
    "@types/node": "<15.0.0",
    "cdk-nag": "^2.15.12"
  },
  "dependencies": {
    "@serverless-stack/cli": "1.4.0",
    "@serverless-stack/resources": "1.4.0",
    "aws-cdk-lib": "2.80.0"
  }
}
